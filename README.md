# LU design studio

Static portfolio website.

## GitHub Pages

1. Extract this ZIP on Mac or Windows.
2. Upload `index.html`, `assets/`, and `README.md` to the root of a GitHub repository.
3. In Settings → Pages, select Deploy from a branch, main, /(root), then save.

No build step is required. Keep `assets/` beside `index.html`.